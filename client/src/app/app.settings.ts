import { environment } from '../environments/environment';

export const Settings = {
    siteName: 'Flight Simulator',
    siteUrl:  environment.siteUrl,
    apiUrl:   environment.apiUrl,
    wsUrl:    environment.wsUrl,
  };
  